{
    "title": "Instagram Follower Counter (Phase 2)",
    "description": "Fetches follower counts for a list of Instagram usernames. Input usernames, get back username + follower count pairs.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "usernames": {
            "title": "Usernames",
            "type": "array",
            "description": "List of Instagram usernames to look up. Do not include the @ symbol.",
            "editor": "stringList",
            "default": [],
            "prefill": ["cristiano", "kyliejenner", "natgeo"]
        },
        "sessionId": {
            "title": "Instagram Session ID",
            "type": "string",
            "description": "Your Instagram sessionid cookie. Required — without it Instagram blocks all requests.\n\nHow to get it:\n1. Log in to Instagram in Chrome\n2. Open DevTools → Application → Cookies → https://www.instagram.com\n3. Copy the value of the 'sessionid' cookie\n\nOr run in the Chrome console:\ndocument.cookie.split(';').find(c=>c.trim().startsWith('sessionid')).split('=')[1]",
            "editor": "textfield",
            "isSecret": true
        },
        "csrfToken": {
            "title": "Instagram CSRF Token (recommended)",
            "type": "string",
            "description": "Your Instagram csrftoken cookie.\n\nChrome console:\ndocument.cookie.split(';').find(c=>c.trim().startsWith('csrftoken')).split('=')[1]",
            "editor": "textfield",
            "isSecret": true
        },
        "proxyConfiguration": {
            "title": "Proxy configuration",
            "type": "object",
            "editor": "proxy",
            "description": "Residential proxies strongly recommended. Without them expect 429 rate limiting.",
            "default": { "useApifyProxy": true, "apifyProxyGroups": ["RESIDENTIAL"] },
            "prefill": { "useApifyProxy": true, "apifyProxyGroups": ["RESIDENTIAL"] }
        }
    },
    "required": ["sessionId", "usernames"]
}
